[package]
name = "praetorium"
version = "2.0.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }

[build-dependencies]
tauri-build = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
codegen-units = 1
